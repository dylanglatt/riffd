# Eval artifacts: multi-GB of audio, all reproducible.
#   audio/  — fetched by eval/fetch_audio.py via riffd's own downloader
#   out/    — regenerated by run_incumbent.py / run_cascade.py
audio/
out/
